ktor {
    deployment {
        port = 10002
    }
    application {
        modules = [ com.teamfinder.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:postgresql://db:5432/teamfinder"
    user = "postgres"
    password = "ab070303"
    maxPoolSize = 10
    migrate = false
}

security {
    jwtSecret = "your-secret-key-change-in-production"
    jwtIssuer = "teamfinder"
    accessTokenExpiry = 3600000 # 1 hour
    refreshTokenExpiry = 2592000000 # 30 days
}

cors {
    allowedOrigins = ["http://localhost:3000", "http://localhost:10002"]
    allowCredentials = true
}

upload {
    directory = "/tmp/uploads"
    maxFileSize = 10485760 # 10MB
    allowedTypes = ["image/jpeg", "image/png", "image/gif", "application/pdf"]
}

email {
    host = "smtp.gmail.com"
    port = 587
    username = "test@gmail.com"
    password = "testpassword"
}

redis {
    host = "localhost"
    port = 10004
}

elasticsearch {
    host = "localhost"
    port = 10005
}